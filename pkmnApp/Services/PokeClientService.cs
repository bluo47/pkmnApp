/*using System.Net.Http.Json;
using System.Text.Json;
using Microsoft.Extensions.Logging;
using PokeApiNet;
//using Shared;

namespace pkmnApp.Services;

public class PokeClientService
{
    public PokeClientService(PokeApiClient pokeClient)
    {
        PokeClient = pokeClient;
    }

    public PokeApiClient PokeClient { get; set; }

    public void ConfigureClient() 
    {
        PokeClient = new PokeApiClient();
    }

    public PokeApiClient GetClient()
    {
        return PokeClient;
    }
}

*/
@namespace pkmnApp
@using PokeApiNet


<MudAutocomplete T="Pokemon" Label="User Name" ValueChanged="OnCboUserChange" Value="@Value"
                 SearchFunc="@SearchCboUsers"
                 Margin="Margin.Dense" Dense="true" Variant="Variant.Outlined"
                 MaxItems="null"
                 ToStringFunc="@(r=> r==null ? null : r.Name)"
                 />
